<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="viewModel"
            type="com.hinuri.youtubesearchapp.ui.SearchViewModel" />
    </data>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white">

    <WebView
        android:id="@+id/webview"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:overScrollMode="never"
        android:fillViewport="true">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/padding_medium">

            <TextView
                android:id="@+id/tv_title"
                style="@style/TEXT_H3B"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                android:text="@{viewModel.videoDetailItem.items.get(0).snippet.title}"
                tools:text="TITLE"/>
            <TextView
                android:id="@+id/tv_view_count"
                style="@style/TEXT_B2"
                android:layout_marginTop="@dimen/padding_small"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tv_title"
                android:textColor="@color/grey_500"
                view_count_convert="@{viewModel.videoDetailItem.items.get(0).statistics.viewCount}"
                tools:text="VIEW COUNT"/>

            <View
                android:id="@+id/view"
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/whiteGrey"
                android:layout_marginTop="@dimen/padding_medium"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tv_view_count"/>
            <TextView
                android:id="@+id/tv_channel"
                style="@style/TEXT_B1"
                android:layout_marginTop="@dimen/padding_medium"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@id/view"
                android:text="@{viewModel.videoDetailItem.items.get(0).snippet.channelTitle}"
                tools:text="CHANNEL TITLE"/>
            <View
                android:id="@+id/view2"
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/whiteGrey"
                android:layout_marginTop="@dimen/padding_medium"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tv_channel"/>

            <TextView
                android:id="@+id/tv_date"
                style="@style/TEXT_B2"
                android:layout_marginTop="@dimen/padding_medium"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@id/view2"
                published_date_convert="@{viewModel.videoDetailItem.items.get(0).snippet.publishedAt}"
                android:textColor="@color/grey_500"
                tools:text="DATE"/>

            <TextView
                android:id="@+id/tv_desc"
                style="@style/TEXT_B1"
                android:layout_marginTop="@dimen/padding_large"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tv_date"
                android:textColor="@color/grey_500"
                android:text="@{viewModel.videoDetailItem.items.get(0).snippet.description}"
                tools:text="DESCRIPTION"/>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.core.widget.NestedScrollView>

</LinearLayout>
</layout>